# warmpet-api 待办执行清单

> 生成时间：2026-08-25
> 基准：本仓库 `/home/devbox/project` 的**实际代码与实际数据库状态**（非历史报告推断）
> 用途：作为收尾阶段的唯一执行依据。每完成一项，勾选并在「验收」栏记录验证方式。

---

## ★ 复核结论（2026-08-27，全量重跑附录 B）

> 本节是最新事实。下方旧章节的数字（迁移 21 条、单测 234/19、e2e 41 等）**已过期**，以本节为准。

**实测基线**（命令见附录 B）：

| 项 | 实测值 |
| --- | --- |
| 迁移 | 已 squash 为**单一 `Baseline1787770870056`**，已应用、无 drift（旧 23 个迁移文件的删除**尚未提交**）|
| 类型检查 | `tsc --noEmit` 0 error（`strict: true` 已全开）|
| Lint | `eslint src test` **0 problems** |
| 单元测试 | **244 例 / 20 套件** 全过 |
| e2e | **55 例 / 3 套件** 全过 |
| 生产代码债务 | `console.*` 0、显式 `any` 0、空 catch 0、真 TODO 0（4 处匹配均为解释性注释）|
| 配置中心 | `game_config` 28 项 |
| 接口 | 107 个 |
| 版本库 | 8 commit；**52 个改动 + 8 个 .ts 未提交** |

**已解决（关闭）**：
- ✅ **P0-4 兑换库存**：`ExchangeItem.stock` + 每人限购，已用量从 `redeem_order` 实时 COUNT，配 `rowsOf` 原子占用
- ✅ **P1-6 对账作业**：`economy/reconcile.service.ts`，`@Cron('10 4 * * *')` + 单实例守卫 + Redis 锁，只读不纠账
- ✅ **P1-3 Lint 清零 / P1-5 e2e**：0 problems；e2e 从 1 例扩到 55 例连真库
- ✅ **P3-1 husky**、**P3-4 helmet + throttler（Redis 存储 + 全局 Guard）**、**P3-5 strict 类型**
- ✅ **迁移 squash 成单一 baseline**（对齐天工做法）
- ✅ 历史三处 `NaN` 隐患（economy 余额、promo `max_uses`、items `consumeOwned`）已由 `common/db/query-result.ts` 的 `rowsOf` 统一收口

**仍未解决**（详见对应章节）：

- ⬜ **P0-3 广告凭证签发并发**——**唯一残留的真实并发漏洞**。`ad-token.service.ts` `issue()` 仍是 `get→判断→incr` 非原子、无锁、无幂等（签发本质不幂等）。已逐一排查同类 check-then-act，其余均已受保护，唯此处裸奔：
  - `pet.consumeDailyCap`：调用点全在 `withLock('pet:'+userId)` 内 → 安全
  - `promo.claim`：真实限额走原子 `UPDATE ... WHERE used_count < max_uses`，Redis 计数仅软频控 → 安全
  - `boost.verifyAd`：先原子核销一次性 nonce（Lua）再发币，受 nonce 稀缺性兜底 → **一旦 P0-3 修好即被 issue() 上限收口**
- ⬜ **P3-2 CI**：无 `.github/`。`tsc/eslint/test/test:e2e` 四条命令已就绪，搬进 Actions 即可
- ⬜ **P3-3 覆盖率阈值**：jest 无 `coverageThreshold`
- ⬜ **P3-6 日志覆盖**：Nest `Logger` 仅 15/178 个生产文件
- ⬜ **P3-7 循环依赖检查**：未接 madge（`package.json` 无 `check:circular`）
- ⬜ **卫生项**：52 个改动 + 8 个 `.ts` 未提交；23 个旧迁移文件的删除也未提交——建议尽快提交，避免与「无 drift」的干净状态漂移
- ▫ 次要：`pet.service.ts` 792 行、`race.service.ts` 615 行，仍是仅有的两个 >400 行文件（P3-8 软上限）

---

## 〇、基线快照（本清单成立的前提）

| 项目 | 立单时状态 | 当前状态（B6/B4 完成后复核） |
| --- | --- | --- |
| 迁移文件 | 12 条，已全部干净应用（见附录 A） | **21 条**（B1 +2、B5 +5、B6/B4 +2）|
| 类型检查 | 通过 | 通过（drift 检查生成空迁移）|
| 单元测试 | 通过（46 个用例 / 4 个套件） | 通过（**234 个用例 / 19 个套件**）|
| e2e 测试 | **未验证**（见 P1-5） | 通过（**41 例**：`/health` + 连真库全链路 40 例）|
| Lint | **188 problems（171 errors / 17 warnings）** | **0 problems**（husky + prebuild 双闸强制）|
| 配置中心项数 | 无（数值硬编码在代码里） | **28 项**（B5 +4、B6/B4 +3）|
| 服务健康 | 正常（PM2 常驻，监听 8080） | 正常，主链路真机冒烟通过 |
| 已建玩法域 | pet / economy / daily / race / items / home / dex / exchange / boost | 追加 **promo（兑换码）**、**gacha（扭蛋）**、**consumable（消耗品）** |
| admin-web 页面目录 | Config、Dashboard、Economy、Login、Player、System、Welcome | 追加 **Marketing（兑换码）** |

结论：**主体功能已建成**，剩余工作集中在两个真实功能缺口（P0）、质量一致性收尾（P1）、以及一批需要逐项核实的加固点（P2）。

> 进度：P0-1、P0-2、P1-3、P1-4、P1-5 已完成；P2 十一项已逐项核实；
> **P2-A 四项修复 + B1/B2/B3 已落地并验收**（2026-08-26）；
> **B5 设计稿未实现玩法 8 项已全部补齐**（2026-08-26）；
> **B6（营销积分发放入口）与 B4（经济 sink 与重标定价）已按拍板落地**（2026-08-26）。
> 清单内**已无待办项**。刻意不做的项见「D. 设计里提过但刻意没做」。

> ⚠ 本仓库存在**并发开发**：清单生成后又落地了 race / ad-token / 微信假登录等改动与第 12 条迁移。
> 执行前请先重跑附录 B 的核对命令，不要直接采信本文记录的数字。

---

## P0 · 功能性缺口（不修则功能不成立）

### P0-1 封禁全域生效 —— ✅ 已完成（2026-08-25）

**原现状**
- `assertNotBanned` 仅存在于 `AuthService.login` 与 `PetService`。
- `JwtAuthGuard` 只验签名、不查库；玩家 JWT 有效期 7 天。
- 后果：账号封禁后，持有存量 token 的客户端仍可正常发奖、扣费、下兑换单，直到 token 自然过期。

**已落地方案（双层）**

- [x] `PlayerStatusGuard`（`src/auth/player-status.guard.ts`）：管准入，只拦写方法，`GET/HEAD/OPTIONS` 放行
- [x] `PlayerStatusService`（`src/auth/player-status.service.ts`）：账号状态校验的唯一出口，由 `AuthModule` 导出
- [x] `EconomyService.apply` 资金兜底：把「账号未封禁」并入余额变更的那条原子 `UPDATE`，绕过守卫的内部调用同样动不了钱
- [x] `PetService.assertNotBanned` 改为委托 `PlayerStatusService`，消除重复实现

> 为什么守卫依赖服务而不是直接注入 `UserRepository`：Nest 在**使用方模块**的注入器里实例化守卫。
> 守卫若直接依赖仓储，每个挂载它的模块都得自己 `TypeOrmModule.forFeature([User])`；
> 依赖 `AuthModule` 导出的服务则零改动，因为各玩法模块本来就已经 import 了 `AuthModule`。

**覆盖范围**：9 个控制器、12 处 `@UseGuards` 挂载点、**28 个写端点**。
比原清单多出 3 个此前漏列的：`POST /ad/token`、`POST /race/reward/double`、`POST /race/revive`。

**已定决策**
- 封禁**只封写、保留读**：钱包、流水、宠物状态仍可查，便于客服解释与用户申诉
- 后台补偿**豁免**：`admin_grant` / `admin_deduct` / `compensation` 三个原因不受封禁拦截，否则无法做封号后的结算与违规追回
- **不做 token 黑名单**：每次写请求查一次库（主键命中）。封禁是安全控制，加缓存会引入「已封禁仍放行」的窗口，而写请求本身必然要打库，多这一次开销可忽略

**验收结果**（冒烟脚本已跑过并删除，永久回归由单测承担）
- 正常账号 28 个写端点：误拦 **0**
- 封禁后同一存量 token 打 28 个写端点：全部 403，漏网 **0**
- 封禁后 13 个读端点：误封 **0**
- 经济域四条路径：封禁发放被拒 / 后台补偿放行 / 后台追回放行 / 解封后恢复，全部符合预期
- 新增 3 个常驻单测覆盖封禁与豁免分支

**顺带修复**：`tsconfig.build.json` 的 `exclude` 补上 `scripts`。
此前 `scripts/` 下放任何 `.ts` 都会抬高编译 rootDir，使产物从 `dist/main.js` 变成 `dist/src/main.js`，导致 PM2 起不来。

---

### P0-2 打通配置中心 —— ✅ 已完成（2026-08-26）

**原现状**
- `game_config` 表存在但 0 行数据；全仓库只有 `admin-config.service.ts` 读写该表，没有任何玩法代码消费它。
- 真实数值全部硬编码在 6 个 `*.config.ts` 文件中。运营在后台改配置，游戏行为不会有任何变化。

**已落地方案**

*可调边界：数值可调，结构不可调*

开放给运营的是「数值」；决定代码分支或历史数据口径的取值留在代码里，因为改它们不是调参而是改语义：

| 留在代码里 | 原因 |
| --- | --- |
| `STAT_MIN` / `STAT_MAX` | DTO 与前端进度条按 0~100 呈现，属结构约束 |
| `BUSINESS_TZ_OFFSET_MS` | 改了会让已落库的业务日键口径漂移 |
| `AD_SCENES` | 每个 scene 对应一条具体兑换代码路径 |
| `daily.tasks[].source` / `dex.entries[].type` | 枚举值各对应一条统计实现，故只在 schema 内收口为已实现的取值 |

*四层结构*

1. **声明**：配置项声明在各自的域文件里（`src/<域>/<域>.config.ts` 的 `PET_CONFIG` / `RACE_CONFIG` / …），每项含 `default` + Joi `schema` + 运营可读 `description`。新增一个可调值只改对应域文件。
2. **汇总**：`src/config/game-config.registry.ts` 合并成总注册表并派生 `ConfigShape`（key → 值类型），使取值有类型安全。
3. **加载**：`src/config/game-config.service.ts`（`@Global()` 模块，与 `RedisModule` 同处理）。
4. **写入**：`admin-config.service.ts` 改为按注册表校验 + 写后失效缓存。

*加载层的三条硬约束*

- **永不因配置问题抛错**。DB 挂了 / 行缺失 / 值非法都回退默认值并告警。
- **加载时二次校验**。写入已校验，但直连 SQL 改库绕得过去；取值时按 schema 再验一遍，且**单项非法只回退该项**，不影响同批其它 key。
- **请求内配置一致**。调用方取一次 `snapshot()` 往下传，避免一个请求内前后读到不同版本。

*配置项清单（21 项，全部支持热改）*

| 域 | key |
| --- | --- |
| pet | `pet.rates`、`pet.growth`、`pet.attrs`、`pet.stages`、`pet.actions`、`pet.daily_cap`、`pet.max_pets_per_user`、`pet.offline`、`pet.comfort` |
| race | `race.tracks`、`race.opponent_count`、`race.revive`、`race.rank_factor` |
| daily | `daily.checkin`、`daily.tasks` |
| boost | `boost.ad_reward`、`boost.stamina_recover`、`boost.speedup`、`boost.ad_token` |
| dex | `dex.entries` |
| exchange | `exchange.items` |

*关键决策与理由*

- **粒度**：一个 key 对应一组常量，value 存整个 JSON。契合既有 `jsonb value` 列与后台 CRUD，21 个 key 而非 80 个散值。
- **校验用 Joi**：项目已用它校验环境变量，不引新依赖。schema 同时守住值域（速率非负、`growth.ratio ≥ 1`、`comfort.factorCap ≤ 0.9`）、禁止未知字段（防运营拼错字段名后静默失效）、跨字段约束（签到 `maxCoin` 不得低于 `baseCoin`）。
- **缓存**：整表一次查询 + 30s TTL + 写后失效，无 Redis pub/sub。当前 PM2 `instances: 1`，写后失效即时生效；将来扩多实例时最坏 30s 收敛，代价小于引入 pub/sub 与额外连接。
- **`invalidate()` 只置标记、不丢快照**。单测发现的缺陷：丢快照会让「刚失效 + DB 恰好不可用」把配置静默跌回代码默认值（奖励、上限突变），比读到略旧的值危险得多。
- **注册项禁止 DELETE**，改用 `POST /admin/config/:key/reset` 恢复默认。删掉只会让它从后台列表消失、运营再也调不到，而玩法照旧走默认值，是纯误操作。DELETE 仅保留用于清理未注册的历史遗留 key。
- **写入只接受注册过的 key**。否则运营打错一个字就存出一条永不生效、也无任何报错的「幽灵配置」。
- **放开 3 个 DTO 的模块级 `@IsIn` 白名单**（`race.trackKey`、`daily.taskKey`、`dex.entryKey`）：白名单在模块加载时快照，会让后台新增的赛道/任务/图鉴条目永远校验不过。改为服务层按当前配置判定未知 key。`boost` 的 `AD_SCENES` 属结构性取值，保留 `@IsIn`。

*接口变化（后台）*

- `GET /admin/config`：响应新增 `registered`（是否代码注册项）、`default`（内置默认值）、`modified`（是否已偏离默认值）三个字段，便于运营看出改了什么、能否恢复。
- `POST /admin/config/:key/reset`：新增，恢复默认值。
- `PUT /admin/config/:key`：非法值或未注册 key 返回 400 并带具体原因（原先任意 JSON 都能写入）。
- `DELETE /admin/config/:key`：注册项返回 400。

**验证结果**

单测 85 个全过（新增 `game-config.service.spec.ts` 与 `admin-config.service.spec.ts` 共 27 例），其中一例专门断言「每个注册项的默认值本身合法」——兜底值不能是坏的。

运行时在 PM2 进程上验证（改库 → 等 TTL → **不重启**）：

| 验证项 | 结果 |
| --- | --- |
| 启动种子灌入 21 项 | ✓ 日志「配置播种完成，新增 21 项」 |
| 改 `race.tracks` 奖励 50 → 999，玩家端读到新值 | ✓ |
| 收紧 `pet.max_pets_per_user` 后建宠被拒（配置驱动业务判断，非仅目录回显） | ✓ |
| 写入非法 `pet.rates` 后玩法不崩、数值非 NaN、日志告警并回退默认 | ✓ |
| 还原配置后行为恢复 | ✓ |

**顺带修掉的问题**

- `settle()` 原先在衰减速率为 0 时会除零得出 `NaN`（速率此前是常量恒为正，配置化后运营可填 0）。已改为用 `Infinity` 表达「永不触底」，并补了单测。
- `DailyService.checkin` / `claimTask` 的 `bizId` 形参是死参数（真正的幂等键由服务端按业务日派生，Redis 层幂等由拦截器直接读 body）。已从服务层签名移除；DTO 保留 `bizId` 及其长度上限，否则 Redis key 无界。
- `eslint.config.mjs` 增加 `argsIgnorePattern: '^_'`：NestJS 的 `@Body()` 有时只为触发校验管道而声明，需要一个表达「有意不使用」的约定。
- `pet.service.spec.ts` 由 `(svc as any)` 改为显式声明私有方法形状，全仓 lint 从 188 降至 101。

**遗留**

- ~~`BUSINESS_TZ_OFFSET_MS` 双定义~~ —— 已在 P1-3 合并到 `common/time/business-day.ts` 单一来源。
- admin-web 的配置页尚未用上新增的 `registered`/`default`/`modified` 字段与 reset 接口，见 P2。

---

## P1 · 质量与一致性

### P1-3 Lint 清零 —— ✅ 已完成（`npx eslint src test` 输出 0 problems）

> 数字演进：初版 92 → 并发开发新增 race / ad-token / wechat 三组测试与一条迁移后 188
> → P0-2 顺带重写 `pet.service.spec.ts` 后 101 → **本轮 0**。

- [x] ~~`pet/pet.service.spec.ts`（69）~~ —— P0-2 中改为显式声明私有方法形状
- [x] ~~`src/daily/daily.service.ts`（3）~~ —— P0-2 中已清理未使用导入与死参数 `bizId`
  - **注意：这不是幂等漏洞。** 签到/任务刻意忽略客户端 `bizId`，改用服务端派生键（`daily:checkin:${day}`）。客户端换个 `bizId` 也领不到第二次奖励。DTO 仍保留 `bizId`（拦截器做 Redis 层去重，且需要长度上限约束 key 大小），**不要「修复」成让服务层使用客户端 `bizId`**。
- [x] **测试文件（84 处）**：统一改为「声明桩的形状 + `as unknown as` 转一次」
  - [x] `race/race.service.spec.ts`（62）：`RacesStub` / `PetStub` / `EconomyStub` / `AdTokenStub` 四个 interface，构造时按真实依赖类型转换。这样将来改 `RaceService` 构造签名或方法名，测试会**编译报错**而不是静默测一个空壳
  - [x] `boost/ad-token.service.spec.ts`（22）：`RedisStub` 用 `Partial<Record<...>>` 只声明各用例真正用到的命令；顺带补一例「每日计数按业务日分桶 + TTL 到次日重置」
  - [x] `economy/economy.service.spec.ts`（2）、`wechat/wechat.service.spec.ts`（2）
  - `jest.fn(async () => x)` 一律改为 `jest.fn(() => Promise.resolve(x))`——桩里没有 await，`require-await` 报的是真问题，不是误报
- [x] **e2e 1 个**：`test/app.e2e-spec.ts` 的 `res.body` 先断言成具名类型再取字段
- [x] **统一 `BUSINESS_TZ_OFFSET_MS`**（P0-2 遗留）：删掉 `pet/pet.config.ts` 的副本，**唯一来源 = `common/time/business-day.ts`**。同时发现 `PetService` 里 `businessDayKey` / `secondsUntilNextBusinessDay` 是公共实现的整段复制，已删除私有版本改为导入——日切口径现在只有一处可改
- [x] **历史遗留**
  - [x] `idempotency.interceptor.ts`（5）：`getRequest<Request & { user?: AuthUser }>()`，`req.body` 单独断言成 `{ bizId?: unknown }`
  - [x] `current-user.decorator.ts`（2）、`current-admin.decorator.ts`（2）：沿用仓库里守卫已有的 `Request & { user }` / `Request & { admin }` 写法，未新造抽象
  - [x] `admin-audit-log.entity.ts`（1）、`admin-idempotency.service.ts`（1）：`unknown | null` → `unknown`（前者本就吞掉后者），补注释说明可空语义
  - [x] `query-audit.dto.ts`（1）：删掉 `@Transform(({ value }) => value)`。全局 `ValidationPipe` 未开 `enableImplicitConversion`，该装饰器是恒等空操作，删除不改变行为
  - 清单里原记的「迁移文件 10 个」「`admin-players.controller.ts` 1 个」在本轮开工前已不复现（并发开发期间被一并解决）
- [x] **强制卡口已落地**（决策：不上 CI/CD，但要强制）。两道，都在本地：
  - **提交卡口**：husky `.husky/pre-commit` → `lint-staged` → 对本次暂存的 `{src,test}/**/*.ts` 跑 `eslint --max-warnings 0`。**不带 `--fix`**——自动修会把「代码有问题」这件事藏起来。已实测：塞一个含 `any` 的文件进暂存区，`lint-staged` 退出码 1，提交被拒。
  - **构建卡口**：`package.json` 新增 `prebuild` → `npm run lint:check`。npm 生命周期会在 `npm run build` 前自动执行，而本项目部署模型是 `npm run build && pm2 reload`，所以这道卡口实际卡在**部署路径**上，比钩子更硬（钩子可被 `git commit --no-verify` 绕过，构建绕不过）。已实测：带错文件时 `npm run build` 退出码 1。
  - 配套命令：`npm run lint:check`（只检查不改，`--max-warnings 0`）、`npm run verify`（lint + tsc + 单测 + e2e 一把过，提交前自查用）。
  - 注意：原有 `npm run lint` 带 `--fix`，是**开发时**用的；核对与卡口一律走 `lint:check`。

**验收结果**：`npx eslint src test` → 0 problems；`tsc --noEmit` 干净；`npx jest` 87 例全过（较清零前 +1，来自新补的业务日分桶用例）；`npm run build` 通过；PM2 reload 后 `/health` 正常，`/pet/create` → `/pet/state` → `/pet/feed` 真机冒烟通过，Redis 额度键落在 `cap:{userId}:20260826:{resource}`，业务日分桶与预期一致（临时数据已清）。

**新增依赖**：`husky`、`lint-staged`（均为 devDependencies）。`npm install` 时 `prepare` 脚本会自动装好钩子，换机器/重装依赖无需手工配置。

---

### P1-4 规格文档同步 —— ✅ 已完成（2026-08-26）

`docs/宠物功能详细规格.md` 曾明显落后于代码，把大量已建成的功能仍标为「待建」。

- [x] **§0.2 已实现接口**：玩家端与后台端点已全部列齐
- [x] **§0.3 数据库现状**：11 张新表 + 迁移清单已补（现 14 条）
- [x] **§0.4 差距总览**：整表已刷新
- [x] **§16 落地排期**：切片 0~5 全部标记完成，并注明与原排期的两处偏差
      （离线收益**没有**广告翻倍；**没有** `track_def` 表，赛道进了 `race.tracks` 配置）
- [x] **§17.3 / §17.4**：`/admin/ledger`、`/admin/wallet/grant`、`/admin/config/*`、
      `/admin/stats/*`、兑换管理均已改为「已建」，并补上本轮新增的
      `GET /admin/reconcile` 与「幂等记录」页
- [x] **补记两个设计决定**（否则后续维护者会误以为是遗漏）
  - [x] §9.3 图鉴**不建 `dex` 表**：进度由 `pet`（等级/数量/亲密度）实时派生，
        只有「奖领没领过」无法推导，故仅 `dex_claim` 落表
  - [x] §11.2 每日奖励用**服务端派生的幂等键**（`daily:checkin:{day}`、
        `daily:task:{day}:{taskKey}`），客户端 `bizId` 只做请求级去重、不参与发币幂等

**顺带修正的失真处**（写文档时逐条比对源码才发现，均已改准）：
- `GET /pet/offline` 与 `/pet/offline/claim` 的出入参（原文写的 `{hasReward, summary, estimateCoin}`
  与 `adToken?` 都不存在；真传 `adToken` 会被 `forbidNonWhitelisted` 直接 400）
- `POST /daily/task/claim` 的参数名是 `taskKey` 而非 `taskId`，且没有 `adToken`
- `daily` 表实际列名（`last_checkin_day`/`streak`/`total_checkins`/`task_day`/`claimed_tasks`）
- `redeem_order` 实际列名与状态枚举（`pending`/`shipped`/`cancelled`，地址是 jsonb 快照而非外键）
- §14.5 那五张「静态配置表」（`furniture_def`/`track_def`/`task_def`/`dex_series_def`/
  `exchange_item_def`）**一张都没建**，全部收进了配置中心
- §12.2 广告钩子表里两个未实现的钩子（离线翻倍、任务领奖翻倍）已明确标注未实现
- **§9 图鉴**：实现只有「成就类」三种进度口径（`maxLevel`/`petCount`/`maxIntimacy`），
  **装扮图鉴与「系列」均未实现**；接口是平铺 `entries` + `entryKey`，不是 `series`/`seriesId`
- **§10 赛跑**：判定实为「战力算分排名次」（`power = speed×2 + endurance`），
  **不是** `finishTime`/S-A-B-C 模型；`mood` 不参与、`clientInput` 未实现、
  对手是现生成 NPC 而非采样他人成绩；`race_record` 无 `finish_time`/`grade`/`ghost_source`
- **§7 换装 / §8 家园**：参数名 `itemKey`（非 `itemId`/`furnitureId`）、`posX`/`posY`（非 `x`/`y`）、
  撤下用 `layoutId`；`equip` 不传 `slot`（由物品定义推导）；装扮家具共用 `item_def` 表
- **§8.1 家园网格未实现**：无 6×6 边界、无占格 w/h、无重叠校验，只存自由坐标
- **§11.1 任务实为 3 个**（`checkin`/`interact`/`play`），设计稿举例的「赛跑 1 场」没有
- **§14.3 玩法域表结构整表刷新**（`item_owned` 无 `type` 列、`dex` 表未建等）

**验收**：`grep -E "🟡|待建|❌|未实现|缺失"` 在规格文档中已无与实际相悖的命中；
所有「未实现」标注均已核对源码，并写明补做时的改动落点。

#### B5 落地后的二次回写（2026-08-26 晚）

上面「顺带修正的失真处」里有 5 条记录的是**当时**的实现状态，B5 把这些设计补齐后，
它们连同规格文档一起失效了。已按新实现重写对应章节，**上面那 5 条只作为变更历史保留**：

| 上面记的旧状态 | B5 后的实际状态 | 重写的章节 |
| --- | --- | --- |
| §7 背景部位 `bg` 无物品等于未启用 | `bg` 已启用（accessory + slot=bg，3 件背景） | §7.1、§7.3 |
| §8.1 家园网格未实现 | 网格 + 占格 + 越界/重叠校验已落地 | §8.1、§8.2、§8.3 |
| §9 只有成就类三种口径、装扮图鉴未实现 | 增 4 种收集类口径（按**种类**数）+ 4 个条目 | §9.1、§9.3 |
| §10 战力算分排名次、不采样他人成绩 | 完赛时间模型 + S/A/B/C + 影子采样真人成绩 | §10.1~§10.4 |
| §11.1 任务实为 3 个、无赛跑任务 | 4 个任务，含 `race`（settle 首次打点） | §11.1 |

同批补进规格文档的还有：
- §7.1 / §十五 / §17.4：后台补发物品（`POST /admin/players/:id/items/grant` +
  `GET /admin/items/grantable`，权限点 `item:grant`）——「补偿只能发币」的说法已删
- §14.4 / §17.4：`redeem_order.shipped_at` / `cancelled_at` 及履约耗时展示，
  并写明**为什么不能只看 `updated_at`**（会被补运单号/改备注覆盖）
- §14.3：`race_record` 三列 + 采样索引、`home_layout` 占格取自 `item_def` 的理由
- §14.5：新增的 4 个配置项（`race.formula`/`race.grade_thresholds`/`race.ghost`/`home.grid`）
- §0.3：给那段 schema 摘录加了「这是切片 1 时的快照」的警示 ——
  它之前那句「玩法域仍均未建」已经和 §0.4 自相矛盾，容易被当成现状读

**同时写进文档的三处已知遗留**（都是有意不修，不是漏）：
- 存量 `home_layout` 坐标全是 (0,0)、按新网格互相重叠；重叠校验只在新增摆放时生效
- 模型切换前的 `race_record` 没有回填 `finish_time`（反推出来是假数据，会污染采样池）
- 存量已履约订单的 `shipped_at`/`cancelled_at` 是按 `updated_at` 近似回填，不作考核依据

---

### P1-5 e2e 测试 —— ✅ 已完成（2026-08-26）

- [x] `test/app.e2e-spec.ts` 烟测（`/health`）
- [x] 决策已拍板：**走路线 A（连真库全链路），但不另开测试库，直接用开发库**
- [x] `test/helpers/e2e-app.ts` 测试夹具 + `test/gameplay.e2e-spec.ts` 全链路用例
      → 现共 **19 例 / 2 套件**

**为什么共用开发库是可接受的**：夹具为每个用例创建独立的临时玩家（openid 带随机后缀），
`afterAll` 按 userId 精确清理 12 张子表 + `user` 行 + 该用户的 Redis 键，不做全表 truncate，
因此不会碰到既有数据。每轮跑完都复查过残留（user/pet/ledger/order/wallet/address 全部归零）。
鉴权不走假登录开关，而是直接用 `JWT_SECRET` 签发玩家令牌 —— 免去为测试打开
`WECHAT_MOCK_LOGIN` 的风险。

**覆盖范围**：鉴权与封禁准入、建宠/互动/记账、离线（封顶 + 重复领取 + 家园加成）、
赛跑（结算幂等 + 装扮不影响判定）、广告凭证核销、每日签到、兑换（余额不足 / 缺地址 /
库存与限购 / 目录余量）。

**边界**：并发与竞态类问题 e2e 覆盖不了（单进程顺序请求），那部分由附录 B 的四个
`p2-verify-*.js` 脚本兜住 —— 它们连真库发真并发。

---

## P2 · 加固项（已全部核实并处理）

**核实已完成（2026-08-26）**，11 项逐条有结论与证据；需要动手的已全部落地，仅 B4 待运营拍板。
下表「是否要改」一栏保留的是**当初的判定**，便于回看决策依据；实际改动见各段落的落地摘要。

### 结论一览

| 项 | 结论 | 是否要改 |
| --- | --- | --- |
| 兑换下单并发 | ✅ 安全（原子扣减 + ledger 唯一键） | 否 |
| 兑换库存 / 限购 | ⚠ **完全没有**（清单原前提有误） | 待拍板 |
| 后台履约 ship/cancel 并发 | ❌ **资损路径，可复现 3/3** | **要改** |
| 广告 nonce（赛跑增值） | ✅ Lua 原子读校删，真一次性 | 否 |
| 广告 nonce（`POST /ad/verify`） | ❌ **完全不校验凭证** | **要改** |
| 离线封顶与重复领取 | ✅ 严格（且不接受客户端申报时长） | 否 |
| 赛跑公平性 | ✅ 纯等级派生，装扮零加成 | 否 |
| 钱包↔流水一致性 | ✅ 单事务 + 唯一写入口 | 否 |
| 定时对账作业 | ⚠ **不存在**，`ScheduleModule` 空载 | 待拍板 |
| 舒适度 → mood | ✅ 端到端通 | 否 |
| 舒适度 → 离线产出 | ❌ **代码未实现，规格却写了** | **要改**（改码或改文档） |
| admin-web 端点对齐 | ⚠ 约 85%，5 个端点未接 | 待拍板 |
| admin-web 配置页 | ❌ 未用上新能力，且 `rowKey` 指向不存在的字段 | **要改** |
| 封禁字段展示 | ⚠ `bannedAt` 未展示（后端已返回） | 要改（小） |
| `item_def` 种子 | ⚠ 结构合理、**价格偏低**（3~4 天买空） | 待运营拍板 |
| 假登录开关 | ✅ **三层硬防护**，生产起不来 | 否 |

---

### A. 确认有缺陷，需要动手（4 项）—— ✅ 全部完成（2026-08-26）

> 落地摘要（原始诊断保留在下方，便于回看当初为什么这么改）：
>
> | 项 | 改法 | 验收 |
> | --- | --- | --- |
> | A1 | `ship`/`cancel` 改条件更新 + 检查影响行数；`cancel` 支持重入补退款 | 7 单测 + `p2-verify-fulfillment.js` 连跑 3/3 由「都成功」转为「一个被拒」|
> | A2 | `/ad/verify` 必填 `adToken`，走 `AdTokenService.consume(scene='ad_reward')` | 4 单测 + 3 e2e（无凭证 / 伪造 / 复用同一枚全部 400 且不发币）|
> | A3 | 离线收益 `×(1 + comfortFactor)`，出参新增 `comfortFactor` | 2 单测 + 1 e2e（摆家具后时薪与可领量同时上升）|
> | A4 | 配置页 `rowKey` 改 `key`、接上 `registered`/`default`/`modified`、加「恢复默认」、注册项不给删 | `max build` 通过；顺带修掉后端 `modified` 的误报（见下）|
>
> **A4 连带修掉一个后端 bug**：`modified` 原先用 `JSON.stringify(a) !== JSON.stringify(b)` 比较，
> 而 jsonb 存取会重排对象键 —— 21 项配置里误报 12 项「已改」。改成顺序无关的深比较后只剩 1 项
> （`exchange.items`，确系 B1 迁移改过）。数组仍按顺序比，因为档位顺序有语义。
> 「已改」标签和「恢复默认」按钮都依赖这个判断，误报会让这两个新功能直接失效，所以必须一起修。

- [x] **A1 · 后台履约并发（资损）**：`AdminExchangeService.ship()` 与 `cancel()` 都是「`findOne` 读状态 → 改字段 → `save()` 整实体」，两者并发时**互不阻塞、都返回成功**。实测（`scripts/p2-verify-fulfillment.js`，复现 3/3）：点发货的运营拿到 `200 status=shipped` 会去仓库发货，同时取消方退了积分、状态改回 `cancelled`、`tracking_no` 被后写的整实体 save 覆盖成 null —— **实物发出 + 积分退回，且库里查不到发货痕迹**。
      修法（与仓库既有惯例一致）：把状态流转改成条件更新并检查影响行数，`UPDATE redeem_order SET status='shipped' WHERE id=$1 AND status='pending'`，affected=0 即抛 400。`EconomyService` 与 `RaceService.settle` 已经是这个写法。
      注：**并发双取消不会退两次钱**，靠的是 ledger 的 `(user_id,biz_id,pool)` 唯一键（`refund:{orderId}`），不是那个不可靠的状态判断 —— 修的时候别把这层去掉。
- [x] **A2 · `POST /ad/verify` 不校验广告凭证**：`AdVerifyDto` 只有 `bizId` + 可选 `scene`（注释写「仅记录用」），`BoostService.verifyAd()` 里没有任何 `AdTokenService` 调用。客户端持 JWT 直接循环调用即可拿满每日广告币（默认 30 币 × 5 次 = 150 币/天），**根本不需要看广告、也不需要先领 nonce**。
      同一份代码里赛跑增值（`/race/reward/double`、`/race/revive`）却强制 `adToken` 并走 Lua 原子核销 —— 两套端点风控强度不一致，是漏改而非设计。
      修法：`/ad/verify` 与赛跑增值对齐，要求 `adToken` 并 `consume(userId, adToken, scene)`。日上限 5 次的量级不大，但这是「客户端说看了就给」的信任模型，上线后是被脚本刷的第一个口子。
- [x] **A3 · 舒适度不影响离线产出**：`computeOffline()` 只吃 `offline_base_at`、等级、`pet.offline` 配置，**不读 `HomeStat`**。而规格 §舒适度写的是「①减缓 mood 衰减 ②提升离线产出」，切片 4 也写了「舒适度回流 mood/离线」。玩家现象：摆满家具后 mood 明显掉得慢，但离线收益一分不变。
      二选一：把 comfortFactor 接进 `computeOffline`，或把规格里「②提升离线产出」删掉。**这是数值设计取向，需要拍板**（家具已经通过 mood 间接影响产出，再直接加成会不会过强）。
- [x] **A4 · admin-web 配置页**：`pages/Config/Kv/index.tsx:136` 用 `rowKey="id"`，而后端 `ConfigListItem` 根本没有 `id` 字段（只有 `key`）—— 每行的 React key 都是 undefined。同时未用上 `registered`/`default`/`modified` 三个字段，也没有 reset 按钮，且对已注册键仍展示「删除」（点了必被后端 400）。
      好消息：`PUT` 返回 400 时**能**看到 Joi 的具体报错，靠的是 `app.tsx` 的全局 errorHandler 读 `data.message`，不是页面级 try/catch。

### B. 需要拍板的项 —— B1/B2/B3/B4/B5/B6 均已拍板并完成

> 落地摘要：
>
> | 项 | 决策与改法 | 验收 |
> | --- | --- | --- |
> | B1 | 加 `stock` / `perUserLimit` 两个**配置字段**（不建表）；已占用量从 `redeem_order` 实时数（排除 `cancelled`），**不存计数器** —— 单一事实来源、不会漂移、取消自动回库。限购判断在扣费之前（拒绝时不碰钱），库存判断在兑换项级锁内（跨玩家串行）；万一超卖走幂等退款自愈 | `p2-verify-stock.js`：8 人抢 1 件 → 成功 1 单、落败者余额分文未动；限购拦第二单不扣费；取消后可再兑。另 2 e2e |
> | B2 | `ReconcileService`：每日 04:10（Asia/Shanghai）校验 `wallet == sum(ledger.delta)`，只读不写。单实例守卫 = `NODE_APP_INSTANCE` 判断 + Redis 锁（`retries: 0`，抢不到就放弃本轮）。另开 `GET /admin/reconcile` 供后台「立即对账」 | 6 单测 + `p2-verify-reconcile.js`：手工改余额不补流水、孤儿流水都被抓出；非 0 号 worker 确认跳过 |
> | B3 | 补了钱包详情（玩家详情抽屉内，按 `wallet:read` 单独控权）、幂等记录查询页（`/system/idempotency`，含后端菜单种子）、对账入口（钱包流水页）。顺带补上 `bannedAt` 与 `lastSeenAt` 展示 | `max build` 通过；三个端点用真实令牌验过返回契约 |
> | B4 | 拍板「A/B/C 三项都要」：**A** 新增两类可无限重复的 sink（消耗品 + 扭蛋）；**B** 收藏品重标定价（目录合计 4350 → **18700**，买断周期 3~4 天 → **约三周**）；**C** 扩目录（皮肤 3→5、配饰 hat 2→4 / neck 2→3、背景 3→4、家具 4→7、新增 5 款消耗品）。调价必须走迁移（播种只补新键、不改存量行） | 21 单测（扭蛋 16 + 消耗品 21 中的相关项）+ 11 e2e + `b4-verify-gacha-consumable.js` 实机 21 项 |
> | B6 | 拍板「A/B/D 都要，D 做入口允许游戏币兑换」：**A** 兑换码（`promo_code`/`promo_redemption` + `POST /promo/redeem` + 后台批次管理页）；**B** 后台批量发放（`POST /admin/wallet/grant-bulk`，按人幂等、部分失败逐条给因）；**D** 兑换目录新增 3 个 game 池虚拟品并**即时到账** | 5 e2e（含「实物仍留 pending 不被误伤」）+ 实机验证「即时发货 + 10 份到账」 |
>
> **为什么库存不建表**：目录本身在 `game_config`（可热改），若另存 `used` 计数器就有了第二个事实来源，
> 取消/补偿/手工改单都要记得回滚它，漏一次就永久漂移。派生计数的代价只是每次下单多两条 COUNT，
> 已用 `idx_redeem_order_key_status` / `idx_redeem_order_user_key_status` 兜住。
>
> **为什么对账只报不改**：自动纠账会把证据一起改掉 —— 余额和流水哪个才是对的，机器判断不了。

- [x] **B1 · 兑换库存 / 限购**：清单原文写的「`redeem_order` 与 `exchange_item` 库存扣减是否原子」，前提不成立 —— `exchange_item` 表不存在（目录早已迁到 `game_config`），`ExchangeItem` 也没有 `stock` 字段。所以不是「会不会超兑」，而是**没有任何库存与限购**：积分够就能无限兑同一件实物（实测同一项连兑 7 单无拦截）。
      曝光面受营销积分发放量约束（`marketing` 池只有后台能发，无任何玩法路径产出），但「运营只备 100 个玩偶、玩家能下 500 单」这种情况现在拦不住。要不要加 `stock` / 每人限购？
- [x] **B2 · 定时对账作业**：全仓 `@Cron`/`@Interval`/`@Timeout`/`SchedulerRegistry` **零匹配**，`ScheduleModule.forRoot()` 引入了但没人用（空载依赖）。`ecosystem.config.js` 注释提到的 `isCronWorker` 守卫在代码里也不存在。
      当前风险偏低：全项目 `UPDATE "wallet"` 与 `INSERT INTO "ledger"` **只出现在 `EconomyService` 一处**，且在同一事务里，所以正常代码路径不会漂移。真正的风险是手工 SQL 改余额不补流水（无 DB 级约束能拦），而这恰恰是最可能发生的场景（就像本轮核实我自己就用 SQL 直接改过测试用户的余额）。
      要不要补一个每日对账作业？加的话必须同时补单 worker 守卫，否则将来扩 `instances > 1` 会重复跑。
- [x] **B3 · admin-web 端点对齐**：约 85%，无「404 级」错配，且前端未用 `{data:...}` 旧格式解包、分页按 `page`/`pageSize` + `{list,total}` 对齐。5 个后端端点前端没用：`GET /admin/config/:key`、`POST /admin/config/:key/reset`、`GET /admin/players/:id/wallet`（service 已定义 `getPlayerWallet` 但零引用）、`GET /admin/idempotency`、`GET /admin/admin-users/:id`。`Welcome` 页无任何数据请求（纯读 `initialState`）。
      除 reset（属 A4）外，其余是「要不要做」而非「做错了」。
- [x] **B5 · 设计稿里已设计但未实现的玩法** —— **8 项全部补齐**（2026-08-26）。
      新增 5 条迁移、7 条 e2e、2 个实测脚本；单测 152、e2e 26、lint 0、drift 空。
      逐项落地与取舍：

  - [x] **B5-1 装扮图鉴**（§9.1）：`DexProgressType` 新增 4 种**收集类**口径
        `ownedSkin` / `ownedAccessory` / `ownedFurniture` / `ownedAll`，`DexService` 经
        `ItemsService.ownedKindCount()` 读 `item_owned`。默认新增 4 个条目（图鉴共 10 条）。
        **按「拥有多少种」而非件数计**：同一件家具能买多份摆满房间，按件数算的话
        「收集 10 种家具」用一种刷十次就达成了。
        没有收集类条目时不查物品表（省掉一次 join，图鉴是每次进页面都拉的接口）。
        迁移 `DexCollectEntries`。**仍不建 `dex` 表**，进度照旧实时派生。

  - [x] **B5-2 赛跑判定模型**（§10.2）：改成**完赛时间模型**。
        `finishTime = distance × paceConstant/(speed × moodFactor) + 耐力缺口 × distance × fadeFactor ± jitter`，
        `moodFactor = 0.8 + 0.2 × mood/100`（**mood 正式进入判定**：心情归零约慢 25%），
        名次 = 完赛时间升序排位，评级 = `finishTime / track.targetTime` 比对 `race.grade_thresholds`。
        `race_record` 加 `finish_time` / `grade` / `ghost_source` 三列；新增配置
        `race.formula` / `race.grade_thresholds` / `race.ghost`，赛道加 `targetTime`。
        **两条刻意的保守取舍**：
        ① **奖励仍按名次折算**（`rewardOfRank` 未动）—— 换判定模型不顺手动经济产出，
          否则线上收益曲线和判定手感两个变量一起变，出问题分不清是哪个。
        ② **评级阈值 A 定在 1.0**（不是设计稿隐含的「快于基准才算达标」）—— 卡在基准以下时，
          达标玩家会因为 ±5% 扰动随机掉档，手感很差。
        `score` 列保留为**战力快照**（不再决定名次），旧记录的 rank 就是按它算的，删了没法解释历史数据。
        **存量记录不回填 `finish_time`**：按旧公式硬凑时间会污染影子采样池。

  - [x] **B5-3 影子对手采样真实成绩**（§10.1）：同赛道、`|petLevel − 自己| ≤ levelBand`、
        回溯 `lookbackDays` 天、**只采已结算**（pending 可能被复活重跑改掉成绩）、排除自己、
        `ORDER BY RANDOM()` 取样（不取最快 N 条，否则同一批「最强成绩」反复出现在所有人面前）。
        三重防刷：**钳制**到 `[baseTime × clampMin, baseTime × clampMax]`（刷榜/改档的极端成绩
        不会让新手直接垫底，摆烂样本也不会让人白捡第一）、**样本不足 `minSamples` 整场退回 NPC**
        （只有 1 个真实影子时名次要么第一要么第二，失真）、**采样异常软失败**退 NPC 不阻断比赛。
        新增索引 `idx_race_ghost_sample`。等级是**按 exp 派生**的，分带用的是派生等级而非 `pet.level` 列。
        > 钳制而非丢弃，是为了保留「有人比你快」的信息量，同时把影响限制在可控范围。

  - [x] **B5-4 家园网格**（§8.1）：新增 `home.grid`（默认 6×6）+ `item_def.grid_w/grid_h` 占格尺寸；
        `place` 校验**完整落在网格内** + **不与已摆放矩形重叠**；坐标可省略 → 自动扫第一个空位
        （不传坐标时全堆在 (0,0) 才是真问题）；房间摆满报明确错误。`GET /home` 出参新增
        `grid` 与每项 `gridW/gridH`。存量摆放坐标**不迁移**：重叠校验只在新增时生效，
        旧数据（全是 (0,0)）不会报错，玩家收纳再摆即自动归位；强行按网格重排会打乱玩家的布置意图。

  - [x] **B5-5 赛跑类每日任务**（§11.1）：`DailyTaskSource` 新增 `race`，默认任务加「完成 1 场赛跑」
        （25 币）。打点在 `RaceService.settle`，**只在状态真正 pending → settled 流转时 +1**
        （否则重复结算能把任务刷满）；计数失败软失败（奖励已发，不该因为计数器挂了而失败）。
        迁移 `DailyRaceTask`。

  - [x] **B5-6 装扮的非购买获取途径**（§7.1）：新增 `POST /admin/players/:id/items/grant`
        （`itemKey` + `qty` ≤ 99 + `reason`），走**独立权限 `item:grant`** 而不是 `player:write`
        —— 发外观和封号是两类风险完全不同的操作，客服该能补发但不该能封号。
        配套 `GET /admin/items/grantable`（精简目录，同样只要 `item:grant`；
        `@RequirePermissions` 是「全部满足」语义，复用原目录接口会因缺 `config:read` 而选不出物品，
        精简字段也顺带不把定价信息暴露给只该发货的人）。**已下架物品也允许补发**（限定款场景）。
        admin-web 玩家列表加「补发物品」入口。
        ⚠ **幂等只到请求级**（`IdempotencyInterceptor` 24h 窗口）：物品发放没有 ledger 那样的
        天然唯一键，为它单开发放流水表在当前量级不划算，口径与 `adjustPet` 补偿一致；
        UI 反馈里带上「发放后持有量」，让运营能自己确认而不是不确定就再点一次。

  - [x] **B5-7 `bg` 背景穿戴部位**（§7.1）：新增 3 款背景（`bg_room` 免费 / `bg_garden` 350 / `bg_starry` 700）。
        **走 `accessory` 类型 + `slot='bg'`**：背景是穿在宠物身上的外观，属换装子系统，
        而 `type` 的职责是分「换装 or 家园」，不是分外观品类 —— 这样零代码改动即生效
        （`WARDROBE_TYPES` 已含 accessory，槽位从物品定义派生）。
        副作用已知并接受：背景计入 `ownedAccessory` 图鉴口径。

  - [x] **B5-8 履约时间落列**（§14.4）：`redeem_order` 加 `shipped_at` / `cancelled_at`，
        `ship`/`cancel` 在条件更新里一起写。**不复用 `updated_at`**：补物流单号、改备注都会刷新它，
        拿它算「下单到发货耗时」会系统性偏短，履约时效没法考核（实测已固化这条）。
        存量终态订单一次性用 `updated_at` 近似回填（比留 null 有用），之后由代码写准确值。
        admin-web 兑换页加「履约时间 / 履约耗时」两列。

- [x] **B4 · 游戏币缺长期 sink** —— ✅ 已完成（2026-08-26，拍板「A/B/C 三项都要」）

      **原问题**：目录合计 3300（B5-7 加背景后 4350）游戏币，日收入保守约 900，**3~4 天买空**；
      买空后只剩报名费 20/50、加速 20、体力恢复 50 这类小额消耗，币越攒越多而商店没东西买。

      **A · 加两类可无限重复的 sink**（问题的根本解 —— 收藏品总有买断的一天，只有可重复项才压得住长期通胀）：

      - **消耗品**：`item_def.type` 扩出 `'consumable'`；效果表落配置中心 `items.consumables`
        （itemKey → `hunger`/`cleanliness`/`mood`/`stamina`/`exp` 增益，只允许非负）。
        新接口 `GET /items/consumables`（目录 + 效果 + 我的持有量）、
        `POST /items/consumables/buy`（可一次买多份）、`POST /items/consumables/use`（一次一份）。
        `PetService.applyConsumable` **刻意不吃冷却、不吃每日上限**：冷却与上限是压制**免费**产出的
        节流手段，对花钱买的道具用同一套，结果是玩家花了钱效果被静默吃掉 —— 这是最难解释的一类问题。
        使用顺序是「先扣道具 → 再施加效果」：效果失败可人工补发，反过来则是能白刷增益的漏洞。
      - **扭蛋**：`gacha.pools` 配置（权重、单抽/十连价、保底抽数、重复品折算币量）；
        `GET /gacha`（含**概率公示**，百分比由服务端实际使用的那份权重实时换算，
        不存在「展示一套、跑另一套」）、`POST /gacha/draw`（1 或 10 连）、`GET /gacha/history`。
        三条必守性质：① **重试不重掷**（结果落 `gacha_draw`，`(user_id,biz_id)` 唯一；
        另有 `delivered` 标记把「掷出了什么」与「是否发到手」分开，崩在发货中途就按原样补发）；
        ② **保底可靠**（计数落 `gacha_state` 而非 Redis —— 那是玩家花币攒出来的进度，
        一次 flush 清零等于抹掉玩家投入）；③ 重复的外观类折算成币（外观第二份对玩家零价值，
        「抽半天全是重复」是抽奖体验最易崩的点；家具不折算，可以摆多份）。
        期望回收刻意低于单抽成本，单测里有一条断言钉住这点，防止后人调权重把它变成印钞机。

      **B · 重标定价**：目录合计 4350 → **18700**，买断周期 3~4 天 → **约三周**。
      调价必须**同时写迁移**（`ItemRepriceAndGachaDeliver1787900000008`）—— 播种是「按 key upsert，
      只补新键、不改存量行」，这是刻意的（否则运营在后台调过的价会被下次重启推平），
      代价就是调价改不动老库。已购买的玩家**不补差价不退款**：涨价前买到的就是便宜买到的，
      回溯调整会让「我的余额为什么变了」变成无法解释的事。

      **C · 扩目录**：皮肤 3→5、配饰 hat 2→4 / neck 2→3、背景 3→4、家具 4→7，新增 5 款消耗品。

      **验收**：扭蛋 16 单测（含概率分布、保底、重放、折算）+ 消耗品 21 单测；
      11 条 e2e；`scripts/b4-verify-gacha-consumable.js` 实机 21 项全通过。

- [x] **B6 · 营销积分没有任何玩家侧获取途径** —— ✅ 已完成（2026-08-26，拍板「A/B/D 都要，D 做入口允许游戏币兑换」）

      **原事实**（当时已逐一验证）：
      - `LEDGER_REASONS` 13 个原因里，没有一个对应「营销积分入账」
        （`interact`/`offline`/`race`/`daily`/`dex`/`ad` 全是 `game` 池产出）
      - `economy.apply` 的 15 个调用点中，向 `marketing` 池发放的**只有**
        `POST /admin/players/:id/wallet/grant`（后台手工）
      - 而 `exchange.items` 默认目录**3 个商品全是 `pool: 'marketing'`**
        （代金券 500 / 马克杯 1200 / 玩偶 2000）

      **后果**：兑换中心对真实玩家目前是**完全兑不动的** —— 玩家怎么玩都攒不到营销积分，
      余额恒为 0，`POST /exchange/redeem` 必然余额不足。B1 做的库存/限购、B5-8 做的履约
      时间、后台履约页，整条链路只有运营手工发过积分的账号才走得通。

      这不是 bug：规格 §6 写明营销积分来源是「线下扫码 / 异业消费（营销触点）」，
      **本来就是站外来源**。缺的是**站外 → 站内的发放入口**，而这个入口没设计过。

      **落地（A/B/D 三项，C「对接 POS/CRM 回调」明确不做 —— 现在没有对接方）**：

      - **A · 兑换码**（主入口，最贴合「线下扫码」且不依赖外部系统）：
        `promo_code` + `promo_redemption` 两表；玩家端 `POST /promo/redeem { code }`、
        `GET /promo/redemptions`；后台 `admin/promo/*`（批量生码、码明细、核销记录、
        按码/按批停用）走**独立的 `promo:read`/`promo:write` 权限** —— 印码等于凭空签发积分，
        谁能印码就等于谁能造积分，这个口子该单独授权、单独审计，不并进 `wallet:write`。
        核销分两步且顺序刻意：① 领用（条件自增 `used_count` + 插核销行，同一事务）
        ② 入账（稳定 `bizId = promo:{codeId}`）。不合成一个事务是因为 `economy.apply`
        自带事务与幂等语义，塞进外层事务会让「唯一冲突 → 幂等回放」不可靠；拆开仍安全，
        因为 ① 是持久领用凭证、② 由稳定 bizId 幂等 —— ② 失败时玩家重提同一个码会命中已有
        领用记录并重走 ②，钱不会少发也不会多发。
        生码字符集去掉 `0/O/1/I/L/U`：码是印在物料上给人手抄的，抄错一位就消耗一次失败额度，
        而失败有每日上限，字符集的可读性直接决定客服工单量。
        频控用 Redis 记每日失败/成功次数，且**「码不存在」与「码已失效」返回同样含糊的提示** ——
        区分开就等于给爆破者一个「码是否存在」的探测器。
      - **B · 后台批量发放**（兜底运营手段）：`POST /admin/wallet/grant-bulk`，
        单次 ≤200 人，幂等键按 `bizId:userId` 逐人派生，**部分失败仍返回 200** 并用
        `failed[]` 逐条给原因 —— 运营只补失败的那几个，不用整批重来。
        admin-web 在钱包流水页加「批量发放」入口。
      - **D · 兑换目录新增 game 池商品**：3 个虚拟礼包（零食礼包 700 / 清洁礼包 900 /
        能量礼包 1500），`ExchangeItem` 加 `grantItemKey`/`grantQty`，兑换后**即时到账**
        （订单直接落 `shipped` + `shippedAt`）。这让兑换中心对**只玩游戏、没有线下触点**的
        玩家也可用，而不是必须先拿到营销积分。

      **顺带修掉一个静默 bug**：`autoFulfill` 原先调的是**带锁**的 `ItemsService.grant`，
      而它自己已在 `pet:{userId}` 锁内 —— Redis 锁不可重入，抢锁重试到超时抛 409，
      又被 `autoFulfill` 的 catch 吞掉，于是「即时到账」从落地起**就没生效过**，
      日志之外毫无迹象。现拆出 `grantUnlocked`（不抢锁，供持锁调用方使用），
      并补了一条 e2e 回归测试钉住「订单直接 shipped + 道具当场进背包」。
      扭蛋发奖同理走 `grantUnlocked`。

      **验收**：5 条 promo e2e（含「已停用码不能兑」「码用尽后第二人被拒」
      「同一人重提走幂等回放而非二次入账」）+ 2 条兑换即时到账 e2e
      （含「实物仍留 pending 不被误伤」）+ 实机验证「即时发货 + 10 份到账」。

### C. 已确认无问题，仅记录结论（3 项）

- [x] **C1 · 离线封顶**：担心的「信客户端申报」不存在 —— `OfflineClaimDto` 只有 `bizId`，且全局 `forbidNonWhitelisted` 会把多传字段直接 400，时长完全由 `offline_base_at → now` 推导。CAP 在预览和领取里各算一遍（领取不信预览）。领取后基准前移，换新 bizId 再领得 400 —— 双保险，不只靠幂等键。e2e 已固化 3 例。
      **顺带修正一处过时注释**：`user.entity.ts:45-46` 写「`elapsed = min(客户端申报, now − last_seen_at)`」，与实现不符，应以 `computeOffline` 为准（已列入 P1-4 文档同步）。
      已知瑕疵（低概率、非 CAP 逻辑问题）：发币与推进基准不在同一事务，若 `users.save` 失败会留下「币已发、基准未推进」，此时客户端**换新 bizId** 重试可双发。正常客户端会用同一 bizId 重试（经济层幂等安全）。
- [x] **C2 · 赛跑公平性**：⚠ **本条的判定描述已被 B5-2 取代**，当前模型见规格 §10.2（完赛时间）。
      仍然成立的结论：三围纯等级派生（`10 + 1.0×(lv−1)` / `10 + 0.8×(lv−1)`），
      `getBattleStats` 不查 `pet_equip`，**装扮零加成**（e2e 已固化：戴上小皇冠后战力与体力上限不变）；
      成绩与名次在 `POST /race/start` 服务端算定并落库，`settle` 只发预存的 `rewardCoin`，
      DTO 无 score/rank/finishTime 入参，客户端无法影响判定。
      随机源是无种子 `Math.random()`，不可复现 —— 客服复盘某一场只能看落库的
      `finish_time`/`grade`/`rank`，无法重算验证。目前可接受，若将来要「回放/申诉」
      能力，需改成 raceId 派生的确定性 PRNG。
- [x] **C3 · 假登录开关**：生产环境是**强制失效**，不是只靠日志。三层：①`env.validation.ts` 的 Joi `.when('NODE_ENV', {is:'production', then: Joi.valid(false)})` —— 生产开着直接**启动失败**；②`configuration.ts` 里 `mockLogin` 由 `WECHAT_MOCK_LOGIN==='true' && NODE_ENV!=='production'` 双条件推导，即使绕过 Joi 也恒为 false；③`WechatService.mockSession()` 在开关关时抛 401「假登录未启用」，明确**不回落**真实微信请求。日志告警只在非生产且开关开启时打印。
      残留注意：`NODE_ENV=staging` 时开关仍可开启 —— 若 staging 接公网需运维管控。

---

## 建议执行顺序

1. ~~**P0-1 封禁**~~ —— ✅ 已完成。
2. ~~**P0-2 配置中心**~~ —— ✅ 已完成。
3. ~~**P1-3 Lint**~~ —— ✅ 已完成（0 problems）。~~**P1-5 e2e**~~ —— ✅ 已跑通，仅剩「是否扩展覆盖范围」待拍板。
4. ~~**P2 逐项核实**~~ —— ✅ 已完成（11 项均有结论与证据，见上）。
5. ~~**P2-A 四项修复**~~、~~**B1 库存限购**~~、~~**B2 对账**~~、~~**B3 后台补齐**~~ —— ✅ 已完成（2026-08-26）。
6. ~~**P1-4 规格文档同步**~~ —— ✅ 已完成。
7. ~~**B5 设计稿未实现玩法（8 项）**~~ —— ✅ 已完成（2026-08-26）。
8. ~~**B6 营销积分发放入口（A/B/D）**~~ —— ✅ 已完成（2026-08-26）。上线阻塞已解除。
9. ~~**B4 游戏币长期 sink 与重标定价（A/B/C）**~~ —— ✅ 已完成（2026-08-26）。

**本清单已无待办项。** 后续若要继续做，候选来自「D. 刻意没做」那张表，
优先级最高的两项是「看广告免费加速 / 免费恢复体力」（改动很轻，广告位已有基础设施）
与「签到/活动自动发装扮」（需要先有活动配置系统，是笔较大的投入）。

### D. 设计里提过但刻意没做（不是遗漏，记此以免反复追问）

| 项 | 状态 | 原因 |
| --- | --- | --- |
| 赛跑 `clientInput`（客户端操作影响判定） | **永不做** | 一采信客户端输入，判定就不再是服务端权威（铁律①） |
| 看广告补签（断签找回） | V2 | 规格 §11.1 已标 V2；当前断签直接重置 |
| 离线收益翻倍（广告） | 未做 | 广告钩子只落在赛跑翻倍/复活 + 直接领币三处；`/pet/offline/claim` 只收 `bizId` |
| 任务领奖翻倍（广告） | 未做 | 同上，`/daily/task/claim` 只收 `{ taskKey, bizId }` |
| 看广告免费加速 / 免费恢复体力 | 未做 | 现为**付费**扣币。改法很轻：`AD_SCENES` 加 scene + 接口收 `adToken` 调 `consume` |
| 图鉴「系列」层（集齐系列额外奖励） | 未做 | 需引入 `seriesId` 与新判定口径，属结构性改动；当前条目是平铺的 |
| 签到 / 活动**自动**发装扮 | 未做 | B5-6 只做了后台**手工**补发（`item:grant`）。自动发放需先有活动配置系统 |
| 排行榜 / 好友 / 分享 | 从未设计 | 规格全文无此需求，影子竞速已替代「和别人比」的心理需求 |

### 小游戏前端需要同步的破坏性变更

B5 这轮有两处出参/入参变化，前端要跟：

| 接口 | 变化 |
| --- | --- |
| `POST /race/start`、`POST /race/revive` | 出参 `opponentScores` → **`opponentFinishTimes`**，新增 `finishTime` / `grade` / `ghostSource`（`player`/`mixed`/`npc`）；`playerScore` 保留但**不再决定名次**，只能当战力值展示 |
| `POST /race/settle` | 新增 `finishTime` / `grade`（旧记录为 `null`） |
| `GET /home` | 新增 `grid`，`placed[]` 与 `items[]` 各项新增 `gridW` / `gridH` |
| `POST /home/place` | `posX`/`posY` 上界从 10000 收到 63，且**越界或重叠会 400**（此前任何坐标都收） |
| `GET /dex` | 新增 4 个收集类条目，`type` 可能是 `ownedSkin`/`ownedAccessory`/`ownedFurniture`/`ownedAll` |
| `GET /daily` | `tasks[]` 新增 `race` 项 |
| `GET /wardrobe` | 目录新增 `slot='bg'` 的 3 款背景，`equipped` 可能出现 `bg` 键 |

B6/B4 这轮**没有破坏性变更**，全是新增接口与新增字段：

| 接口 | 变化 |
| --- | --- |
| `POST /promo/redeem`、`GET /promo/redemptions` | **新增**。营销积分的玩家侧入口。码不存在/已失效统一 404 且提示含糊（防探测）；同一人重提同一码返回 201 + `duplicated: true`（幂等回放，不二次入账），**不是报错** |
| `GET /gacha`、`POST /gacha/draw`、`GET /gacha/history` | **新增**。`GET /gacha` 的 `odds[]` 是**必须展示**的概率公示（虚拟道具抽取合规要求）；`draw` 只接受 `times` 为 1 或 10；产出里 `converted: true` 表示重复的外观类已折算成币 |
| `GET /items/consumables`、`POST /items/consumables/buy`、`POST /items/consumables/use` | **新增**。`use` 一次只用一份（批量会让「喂到上限该停」变成服务端猜测，多喂的被 clamp 掉就是玩家道具凭空消失） |
| `GET /wardrobe` | 目录新增第 4 款背景（星夜露台）与 2 款皮肤、2 款配饰、3 款家具 |
| `GET /exchange` | 目录新增 3 个 **`pool: 'game'`** 虚拟礼包，兑换后**即时到账**（订单直接 `shipped`），前端不必再引导玩家「等发货」 |
| 全部收藏品价格 | **已重标定**（目录合计 4350 → 18700）。前端若缓存过价目表需清缓存，价格一律以接口返回为准 |

---

## 附录 A：已应用迁移（21 条，按时间序）

> P0-1、P0-2、P2-A 均未新增迁移：封禁字段与 `game_config` 表此前已存在，改动都在应用层。
> P2-B1（兑换库存/限购）新增两条：一条建 COUNT 用的索引，一条把存量 `exchange.items`
> 配置补上 `stock`/`perUserLimit`（缺字段会被严格 schema 判成脏配置、静默回退默认值，
> 导致后台看到的值与实际生效的值不一致）。B5 新增五条，B6/B4 新增两条。
>
> **新配置项（`items.consumables`、`gacha.pools`）为什么没有对应迁移行**：
> 它们是全新 key，`GameConfigService` 启动时会把注册表里缺失的 key 按代码默认值补种进
> `game_config`，所以后台立刻可见可热改。需要写迁移的是另两种情况：给**已有** key 的
> schema 加必填字段（存量行会被判非法并静默回退默认值），以及改**已有**数据行
> （如本轮的重标定价 —— 播种只补新键、不动存量行）。
>
> **配置类迁移为什么必须写**：新注册的 key 若不落 `game_config` 行，后台配置页看不到它
>（`AdminConfigService.list()` 只列 DB 行），运营就没法热改；已有 key 的 schema 加了必填字段时，
> 存量行会被判非法并静默回退默认值。两种情况都是「代码以为生效了，运营看到的是另一回事」。

```
1787601751796-InitUserPet
1787617731060-AdminRbac
1787621011377-PetMultiAndStats
1787622024606-UserBanStatus
1787623652203-EconomyWalletLedger
1787625010537-DailyAndOffline
1787625363122-RaceRecord
1787625993161-ItemsHomeDex
1787626288822-ExchangeAddress
1787626627685-GameConfig
1787627335409-AlignJsonbDefaults
1787659151481-RaceAdBoost
1787900000000-ExchangeStockIndex     # B1：库存/限购 COUNT 的支撑索引
1787900000001-ExchangeStockConfig    # B1：给存量兑换目录补 stock / perUserLimit
1787900000002-RaceFinishTimeModel    # B5-2/3：finish_time/grade/ghost_source 三列 + 采样索引 + 3 条新配置 + 赛道 targetTime
1787900000003-DexCollectEntries      # B5-1：给存量图鉴补 4 个收集类条目
1787900000004-HomeGridAndBackgrounds # B5-4/7：item_def 占格列 + 3 款 bg 背景 + home.grid 配置
1787900000005-DailyRaceTask          # B5-5：给存量每日任务补「完成 1 场赛跑」
1787900000006-RedeemOrderFulfillTimes# B5-8：shipped_at/cancelled_at 两列 + 存量近似回填
1787900000007-PromoAndGacha          # B6-A/B4-A2：promo_code/promo_redemption + gacha_draw/gacha_state 建表
1787900000008-ItemRepriceAndGachaDeliver # B4-B/C：收藏品重标定价（存量行）+ gacha_draw.delivered 交付标记
```

## 附录 B：核对命令

```bash
# 一把过：lint + 类型 + 单测 + e2e（提交前自查）
npm run verify

# Lint 只检查不改（npm run lint 带 --fix，核对时用 lint:check）
npm run lint:check

# 类型检查
npx tsc --noEmit

# 单元测试 / e2e
npm test
npm run test:e2e

# 迁移状态（应无待执行）
npm run typeorm -- migration:show

# Drift 检查：应生成空迁移，确认后立即删除该文件
npm run migration:generate -- src/migrations/_DriftCheck

# 配置中心种子是否齐全（应为 28，与代码注册表项数一致）
psql "$DATABASE_URL" -c 'select count(*) from game_config;'

# 哪些配置已被运营改过（value 与代码默认值不同的项）
curl -s -H "Authorization: Bearer $ADMIN_TOKEN" localhost:8080/admin/config \
  | jq '.list[] | select(.modified) | {key, value}'

# 是否有脏配置正在被回退（启动与每次缓存重载时会打 ERROR）
pm2 logs warmpet-api --nostream --lines 500 | grep 'GameConfig'

# P2 验收脚本（连真库，自建自清临时用户；改完代码要先 npm run build）
node scripts/p2-verify-exchange.js      # 兑换下单：并发扣费 / 幂等回放 / 不限量项
node scripts/p2-verify-fulfillment.js   # 后台履约 ship/cancel 竞态（A1）
node scripts/p2-verify-stock.js         # 库存与每人限购、取消回库、8 人抢 1 件（B1）
node scripts/p2-verify-reconcile.js     # 对账抓漏、定时任务注册、单实例守卫（B2）
node scripts/b5-verify-admin-grant.js   # 后台补发物品 + 履约时间落列（B5-6 / B5-8）
node scripts/b5-verify-race-ghost.js    # 完赛时间判定 + 影子采样/钳制/兜底、mood 参与（B5-2 / B5-3）
node scripts/b4-verify-gacha-consumable.js # 扭蛋概率公示/保底/重放不重掷 + 消耗品 + game 池即时到账（B4 / B6-D）
```

> 这七个 `*-verify-*.js` 是**可复跑的回归证据**，不是一次性脚本：它们直接连开发库、
> 自建自清临时用户，跑完会把配置与数据还原。改动经济/履约/库存/赛跑判定相关代码后建议全跑一遍
> —— 单测挡不住并发与采样问题，这类 bug 只有真库真数据才暴露。
>
> `b4-verify-gacha-consumable.js` 打的是**真实 HTTP、跑在 PM2 常驻进程上**，这一点是刻意的：
> 单测的桩不会暴露「Redis 锁重入」「配置没播种」「迁移没跑」这类只在真实进程里出现的问题
> —— 兑换的「即时到账」就是这样静默失效了一整轮（详见 B6 落地说明）。
>
> `b5-verify-race-ghost.js` 里有个容易踩的坑已固化成注释：**等级是按 exp 派生的**，
> `pet.level` 那一列不作数，灌影子样本前得先问服务端「这只宠现在算几级」，
> 否则样本全落在等级带外、采样恒为空而看起来像「功能没生效」。

> 注意：`src/data-source.ts` 的 `entities` 使用 glob 通配（`src/entities/*.entity.ts`）。
> 不要改回显式数组——曾因显式列举导致 `migration:generate` 误判未注册的表并生成 `DROP TABLE`。
